#include "binary_trees.h"

/**
 * binary_tree_size - measures the size ofbinary tree
 * @tree: node of measures the size
 * Return: 0 if NULL
 */
size_t binary_tree_size(const binary_tree_t *tree)
{
	size_t m = 0;

	if (!tree)
		return (NULL);

}
